{
  "name": "cpuritan-schedule-worker",
  "version": "1.0.0",
  "private": true,
  "description": "Cloudflare Worker that proxies schedule.json edits to the GitHub Contents API.",
  "type": "module",
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "secret:put": "wrangler secret put GH_TOKEN"
  },
  "devDependencies": {
    "wrangler": "^3.80.0"
  }
}
